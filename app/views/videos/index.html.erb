<% provide(:title, "All videos") %>

<h1>All videos</h1>

<%= will_paginate @videos, renderer: BootstrapPagination::Rails, bootstrap: 3 %>

<ul class="videos">
  <%= render @videos %>
</ul>

<%= will_paginate @videos, renderer: BootstrapPagination::Rails, bootstrap: 3 %>
