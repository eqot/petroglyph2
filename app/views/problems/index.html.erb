<h1>Problems</h1>

<p><%= link_to "Add new problem", new_problem_path %></p>

<ul>
	<% @problems.each do |problem| %>
	<li>
		<%= link_to problem.title, problem_path(problem.id) %>
		<%= link_to "[Edit]", edit_problem_path(problem.id) %>
	</li>
	<% end %>
</ui>
